# 留言板 · 不该进仓库的东西
#
# 这个项目本身不存任何密钥 —— 云端只下发"公开配置"（应用域名 + 公开标识），
# 真正的密钥留在服务器那边。所以下面这些是防御性的，防止以后手滑。

# 环境变量（真密钥只允许出现在这里，且永不提交）
.env
.env.local

# 各种密钥文件
*.key
*.pem

# 依赖（如果哪天引入 npm 包）
node_modules/

# 系统杂物
.DS_Store
Thumbs.db
desktop.ini
